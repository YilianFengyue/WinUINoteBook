<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="App2.Pages.BrowserPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 浏览器工具栏 -->
        <CommandBar Grid.Row="0" DefaultLabelPosition="Right">
            <AppBarButton ToolTipService.ToolTip="返回">
                <AppBarButton.Icon>
                    <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE72B;"/>
                </AppBarButton.Icon>
            </AppBarButton>
            <AppBarButton ToolTipService.ToolTip="前进">
                <AppBarButton.Icon>
                    <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE72A;"/>
                </AppBarButton.Icon>
            </AppBarButton>
            <AppBarButton ToolTipService.ToolTip="刷新">
                <AppBarButton.Icon>
                    <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE72C;"/>
                </AppBarButton.Icon>
            </AppBarButton>
            <AppBarButton ToolTipService.ToolTip="主页">
                <AppBarButton.Icon>
                    <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE80F;"/>
                </AppBarButton.Icon>
            </AppBarButton>
        </CommandBar>

        <!-- 浏览器内容区域 -->
        <Grid Grid.Row="1" Margin="20">
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="40">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="20">
                    <FontIcon FontFamily="Segoe Fluent Icons" 
                              Glyph="&#xE774;" 
                              FontSize="64"
                              Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"/>
                    <TextBlock Text="浏览器页面"
                               FontSize="24"
                               FontWeight="SemiBold"
                               HorizontalAlignment="Center"/>
                    <TextBlock Text="这里将实现Web浏览功能"
                               FontSize="16"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                               HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Page>